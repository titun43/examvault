<?xml version="1.0" encoding="utf-8"?>
<!--
  =============================================================================
  EXAMVAULT - AndroidManifest.xml
  =============================================================================
  Package: com.examvault.education
  AdMob App ID, Internet permission, সব necessary permissions configure করা আছে।
  =============================================================================
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ==================== PERMISSIONS ==================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Phone Auth এর জন্য -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    
    <!-- Notifications এর জন্য (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Storage এর জন্য -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- Camera (profile photo) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="ExamVault"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="false">

        <!-- ==================== ADMOB APP ID ==================== -->
        <!-- Real AdMob App ID: ca-app-pub-1742730064755213~7890219994 (from build.gradle) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />

        <!-- ==================== DISABLE CRASHLYTICS AUTO-COLLECTION ==================== -->
        <!-- Crashlytics auto-initialises a native crash pipeline at app start.
             If that pipeline misconfigures (missing symbols, version mismatch,
             network issue) it can crash the app natively with "ExamVault keeps
             stopping" — and because it's native, Dart's runZonedGuarded can't
             catch it. Disabling auto-collection here stops the native pipeline
             from running. We still keep the dependency for manual reporting. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <!-- ==================== DISABLE ANALYTICS AUTO-COLLECTION ==================== -->
        <!-- Same reasoning: Analytics auto-collects at native level and can
             crash if Google Play Services / measurement is misconfigured. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />

        <!-- ==================== FIREBASE MESSAGING ==================== -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="examvault_notifications" />

        <!-- ==================== MAIN ACTIVITY ==================== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ==================== NOTIFICATION CHANNEL ==================== -->
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" android:exported="false" />
        <receiver android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ==================== ADMOB AUTO-INIT ENABLED ==================== -->
        <!-- Previously AUTO_INIT_IMPLICIT was set to false (manual init from
             Dart). But that was a likely root cause of the native SIGSEGV
             crashes: MobileAds.instance.initialize() called from Dart can
             race with the Flutter engine / Activity lifecycle and crash below
             Dart's runZonedGuarded. Letting the SDK auto-init natively (the
             Google-recommended path) is more stable — the SDK initialises at
             the correct native lifecycle moment. AdMobService.initialize()
             is still called from Dart (idempotent — safe to call after
             auto-init) so _initialized is tracked correctly. -->
        <!-- AUTO_INIT_IMPLICIT block removed to enable native auto-init -->

        <!-- ==================== DISABLE FCM AUTO-INIT ==================== -->
        <!-- Firebase Messaging auto-initialises at app start and can crash
             natively if Google Play Services is missing or outdated. We init
             FCM manually from NotificationService.initialize() inside a
             try/catch, so auto-init is not needed. -->
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="false" />
    </application>

    <!-- ==================== QUERIES ( Razorpay, etc) ==================== -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>
</manifest>
